# 비브레이브 자동화 프로젝트 의존성

# API 연동 (B·C 단계 — 네이버 트렌드·쇼핑 API)
requests>=2.31

# .env 파일로 API 키 관리 (선택, 없어도 동작)
python-dotenv>=1.0

# 네이버 커머스 API 인증 서명 (bcrypt 기반)
bcrypt>=4.0

# 이미지 해상도 체크, 등록 파이프라인 품질 경고용
Pillow>=10.0

# 로컬 대시보드 (webapp/)
Flask>=3.0
